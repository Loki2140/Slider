const imgages = document.querySelectorAll(".slider_item");
const navButtons = document.querySelectorAll(".nav_buttons");
const nav_points = document.querySelectorAll(".nav_points");


const slider = new Slider(
    imgages,
    navButtons,
    nav_points
);